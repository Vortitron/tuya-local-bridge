# Secrets — local keys and a refresh token granting full control of the home.
session.json
provenance.json
*.tmp

__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage

# Local assistant session state, not project content.
.claude/
